import { list } from '../model/userModel';

export function findAll(res) {
    list();
    //.then( (response) => {
      //  res.status(200).send(response);
    //}).catch( (error) => {
      //  res.status(500);
    //});
}
